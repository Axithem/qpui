import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-buzzers',
  imports: [RouterLink],
  templateUrl: './buzzers.html',
  styleUrl: './buzzers.scss'
})
export class Buzzers { }
